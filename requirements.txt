streamlit==1.31.0
pandas==2.2.0
plotly==5.18.0
scikit-learn==1.4.0
statsmodels==0.14.1
xgboost==2.0.3
numpy==2.1.1 
shap==0.46.0
matplotlib==3.7.2
seaborn==0.12.2
joblib==1.3.2

# ── Supabase backend (new) ──────────────────────────
supabase==2.9.1
python-dotenv==1.0.1

# ── Desktop tracking (used by tracker.py / burnout_widget.py) ──
# These were missing from requirements.txt before this migration too —
# unrelated to Supabase, just filling a pre-existing gap.
psutil==5.9.8
pynput==1.7.7
pygetwindow==0.0.9        # Windows only
pystray==0.19.5           # Windows only, used for the system tray icon

# ── Optional: only needed if you add the FastAPI layer ──
# fastapi==0.115.0
# uvicorn==0.30.6